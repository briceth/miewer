<div class="wrapper">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-md-6 col-md-offet-4">
        <% @movies.each do |movie| %>
          <%= movie.title %>
          <%= movie.description %>
          posted by <%= movie.user.email %>
        <% end %>
      </div>
    </div>
  </div>
</div>
