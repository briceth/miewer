<div class="wrapper">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 col-md-6 col-md-offset-3">
          <%= @actor.first_name %>
          <%= @actor.last_name %>
          <%= @actor.profession %>
          <%= @actor.age %>
          <%= @actor.nationality %>
          <%= @actor.biography %>
          <%= render partial: "reviews/reviews", locals: {reviewable: @actor} %>
          <%= render partial: "reviews/form", locals: {reviewable: @actor} %>
      </div>
    </div>
  </div>
</div>
